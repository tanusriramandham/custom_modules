from math_utils import is_prime
from string_utils import count_vowels
s=input("enter the string you want to count the vowels init:")
n=int(input("Enter any number to check if it is a prime number or not"))
print(is_prime(n))
print(count_vowels(s))