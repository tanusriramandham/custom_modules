import statistics
def cal_mean(numbers):
    return f"the mean value is: {statistics.mean(numbers)}"
def cal_median(numbers):
    return f"the median value is: {statistics.median(numbers)}"
def cal_mode(numbers):
    return f"the mode value is: {statistics.mode(numbers)}"